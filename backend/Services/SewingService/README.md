# SewingService

Shop-floor sewing API (lines, assignments, targets, daily hourly output, balances, panel receive). Port **5130**.

```powershell
dotnet run --project Services\SewingService\SewingService.API --urls http://0.0.0.0:5130
```
